// dsh 插件家族共享层 —— 升级链空步（单一事实源）。
//
// 版本链以步骤为刻度：没有数据要改的版本也要占一步，否则存储刻度永远停在旧值上。
// 历史上各包为每个空版本手写一个空函数（notifier migrateToV025、mcp/provider migrateToV026），
// 逐字重复且每个都要配一条“文件不动”用例。本模块是唯一的空步实现，各包只登记版本号，
// 不再为新版本加空函数。

/**
 * 空步（异步链用）：立即完成，不碰任何存储。
 *
 * 调用方把版本号写在自己的步骤表里（fromVersion/targetVersion），run 统一指到本函数。
 * 故意不收任何参数：空步不需要外部依赖，收了就是让调用方误以为它会用。
 *
 * @returns {Promise<void>} 恒为完成态的 Promise。
 */
export async function tickUpgradeVersion() {
  // 空实现：只推进调用方步骤表里的刻度，本函数自己不读写任何文件。
}

/**
 * 空步（同步链用）：什么都不做，立即返回。
 *
 * 和上面的异步版是同一语义，只是同步形态：有些包的升级链是同步的（run 返回空类型），
 * 把异步函数塞给同步链会被门禁判红（浮起的 Promise 等于埋了个不定时炸弹），
 * 所以同步链用这个，异步链用上面那个。两个都不读写任何文件。
 */
export function tickUpgradeVersionSync() {
  // 空实现：同上，同步形态。
}
