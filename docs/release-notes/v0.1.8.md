This release contains 4 new features, 6 bug fixes, 6 other changes.
本次发布包含 4 项新功能、6 项修复、6 项其他改动。

### New Features / 新功能

- [dsh-web-file-preview] U8v2 多文件浏览：Modal 内「← 返回」历史栈 + Markdown 相对引用重写与内嵌图 blob 化（复用传输通道） / U8v2 multi-file browsing: in-modal "← Back" history stack, plus Markdown relative-reference rewriting and inline-image blobbing (reusing the transport channel)
- [dsh-notifier] 功能批客户端：免打扰紧急例外面板、未读角标 / Feature-batch client: Do-Not-Disturb urgent-exception panel and unread badge
- [dsh-notifier] 功能批宿主端：审批超时提醒、免打扰紧急例外、完成风暴聚合、错误合并详情 / Feature-batch host: approval-timeout reminder, DND urgent exception, completion-storm aggregation, merged-error details
- [dsh-notifier] 合并窗口统一可配置（0=关）+ 通知历史清理机制 + 免打扰拦截记录 / Unified configurable merge window (0=off), notification-history cleanup, and DND block log

### Bug Fixes / 修复

- [dsh-notifier] SSE 可靠性与多标签去重（data 心跳 / seq 回放 / Watchdog / 主标签租约）+ turn-stopping 同轮去重 / SSE reliability and multi-tab dedup (data heartbeat, seq replay, watchdog, primary-tab lease) plus turn-stopping same-round dedup
- [dsh-notifier] 错误脱敏补全（sanitizeErrorText 漏网）+ 局域网直连 403 引导 + iOS 无 Notification 能力说明 / Error-text redaction gap closed (sanitizeErrorText miss), LAN-direct 403 guidance, and iOS no-Notification capability note
- [dsh-notifier] 浏览器端口适配：Pointer 拖拽 / 窄屏 / 触控目标 / 焦点可见性 / Browser-port adaptation: pointer drag, narrow-screen, touch targets, focus visibility
- [dsh-web-file-preview] 安全加固：git 命令 + 传输安全 + 竞态修复（依据安全评审） / Security hardening: git command, transport security, race-condition fixes (per security review)
- [dsh-web-file-preview] 移动端触控与 a11y：捏合 / 触控目标 / 安全区 / 焦点管理 / Mobile touch & a11y: pinch, touch targets, safe-area, focus management
- [dsh-web-file-preview] 体验一致改进：锁滚动 / 剪贴板降级 / 识别边界 / diff 状态 / UX consistency: scroll lock, clipboard fallback, recognition bounds, diff state

### Other Changes / 其他改动

- [README] 中英配对 README（新增英文版 + 语言切换），重组安装 / 升级 / 相关项目章节，去除硬编码版本号 / Bilingual README (English version + language switcher), restructured install/upgrade/related sections, dropped hardcoded version pins
- [dsh-plugins-all] 下架已停维护的 dsh-skill-explorer（npm 已 deprecate，改用 web-ui 实现）；聚合包 8→7 子包并重生成聚合 patch / Retired unmaintained dsh-skill-explorer (npm deprecated; use web-ui impl); aggregator down to 7 sub-packages with regenerated patch
- [dsh-lan-proxy] 移除 __dshTrustedLan 魔法变量与宿主补丁脚本 / Removed the __dshTrustedLan magic variable and host patch script
- [dsh-web-file-preview] 安全模型补充局域网部署高危告警；归档技术债务清单与自研轮子对照 / Security model: added LAN-deploy high-risk warning; archived tech-debt list and in-house-vs-library comparison
- [dsh-opencode-usage] 合入 usage-provider-adapter 设计文档并修复主文件断链 / Merged the usage-provider-adapter design doc and fixed a broken main-file link
- [dsh-notifier] 配置样例补全：doneMergeWindowMs / historyMaxAgeDays，history 路由支持 DELETE / Config samples completed: doneMergeWindowMs / historyMaxAgeDays; history route now supports DELETE

---

Curated bilingual release notes for v0.1.8 — merged, optimized, and trimmed from the raw commit log by the release agent. / v0.1.8 人工校订的双语发布说明——由发版 agent 在原始提交记录基础上合并、优化与精简。
