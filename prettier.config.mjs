/**
 * 仓库 Prettier 配置。
 *
 * 只写 `printWidth`，其余全部跟默认——Prettier 的默认值本身就是它的规则集（双引号、分号、
 * 尾逗号 all、2 空格缩进、箭头函数总是带括号），而 `packages/` 下的现有代码正是这个风格。
 * 把默认值显式抄一遍，只会在升级 Prettier 时把「跟着默认走」变成一份需要人工比对的清单。
 *
 * 100 而不是默认的 80：现有代码里有大量 ≤110 列的单行调用与签名，80 会把它们成片拆开，
 * 让格式化的 diff 淹没真实改动。
 *
 * 生效范围见 `.prettierignore`；全仓一次性格式化与存量清理见 #762。
 */
export default {
  printWidth: 100,
};
