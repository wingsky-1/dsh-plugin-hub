# dsh-verify-isolated bundle patch：注册隔离验证 skill provider（宿主半）。
# 插件 apply 时经 ctx.skills.registerProvider 注册 dsh-verify-isolated skill，
# 使 profile 内会话可用该 skill（临时 DSH_HOME + 独立 profile 双重隔离验证）。
- insert:
    - id: skill-dsh-verify-isolated
      name: '@wingsky-1/dsh-verify-isolated'
