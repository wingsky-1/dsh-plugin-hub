/**
 * dsh-notifier events 域 —— **对上依赖申报表**。
 *
 * 本域声明「我需要外部什么」，不关心谁满足它——装配由组合根完成。依赖集中在此，
 * 实现块经本文件引用，不直连他域。
 */
/**
 * 展示强度：通知请求的字段之一。
 *
 * 与投递域同源而不是各定义一遍——同一个枚举在两侧各写一份，就会出现「一处认
 * warning、另一处不认」的静默失配，而失配的表现是通知少了一个颜色，没人会去查。
 */
export type { NotifySeverity } from "../channels/interface.ts";
