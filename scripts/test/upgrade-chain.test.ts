/**
 * Standard test:scripts entry for the shared upgrade-chain contract.
 * The assertions live beside the shared module so the coverage scope remains explicit.
 */
import "../../shared/test/upgrade-chain.test.ts";
