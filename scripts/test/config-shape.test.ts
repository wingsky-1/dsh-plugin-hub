/**
 * Standard test:scripts entry for the shared settings-namespace contract.
 * The assertions live beside the shared module so the coverage scope remains explicit.
 */
import "../../shared/test/config-shape.test.ts";
